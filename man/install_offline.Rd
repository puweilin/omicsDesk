% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install.R
\name{install_offline}
\alias{install_offline}
\title{Install omicsDesk and everything it needs from a bundle}
\usage{
install_offline(bundle_dir, lib = NULL, force = FALSE, quiet = FALSE)
}
\arguments{
\item{bundle_dir}{Path to the unzipped bundle.}

\item{lib}{Library to install into. Defaults to the first entry of
\code{.libPaths()}, normally the user library.}

\item{force}{Reinstall packages that are already present at the
bundled version.}

\item{quiet}{Pass \code{quiet = TRUE} to \code{install.packages()}.}
}
\value{
Invisibly, a list with \code{lib}, \code{installed} (package names) and
\code{home} (the desk folder pandoc and the BUNDLE record went to).
}
\description{
A bundle is the directory (or unzipped archive) that
\code{tools/build_bundle.R} produces on a networked machine: a local
CRAN-style repository holding omicsCore, omicsApp, omicsDesk and their
whole dependency closure as packages built for one platform and one R
minor version, plus pandoc and a \code{BUNDLE} record. This function is the
same code as the bundle's own \code{INSTALL.R}; it exists so that a machine
that already has omicsDesk can upgrade from a newer bundle without
leaving R.
}
\examples{
\dontrun{
install_offline("~/Downloads/omicsDesk-0.1.0-R4.5-windows")
}
}
