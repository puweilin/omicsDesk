% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/launch.R
\name{launch}
\alias{launch}
\title{Launch omicsApp on this machine}
\usage{
launch(
  project = NULL,
  data_dir = NULL,
  port = NULL,
  browser = interactive(),
  workers = 2L,
  online = FALSE,
  quiet = FALSE,
  ...
)
}
\arguments{
\item{project}{Optional \code{.omp} file to open on start.}

\item{data_dir}{Where projects are stored. Defaults to
\code{desk_paths()$projects}.}

\item{port}{Port for the local server; \code{NULL} lets Shiny pick one.}

\item{browser}{Open the default browser. Default \code{interactive()};
the bundle's launchers pass \code{TRUE}.}

\item{workers}{Background R processes for long analyses, as in
\code{\link[omicsApp:launch]{omicsApp::launch()}}.}

\item{online}{Set \code{TRUE} on a networked machine to allow the KEGG
auto-refresh and live \code{refresh_geneset_cache()} calls.}

\item{quiet}{Skip the one-line banner.}

\item{...}{Passed on to \code{\link[omicsApp:launch]{omicsApp::launch()}}.}
}
\value{
Invisible \code{NULL}, once the app has stopped.
}
\description{
Starts the omicsApp interface with the environment an offline laptop
needs, and puts everything back when the app stops:
}
\details{
\itemize{
\item projects and the autosave go to \code{desk_paths()$projects} instead of a
temporary directory, so they are still there tomorrow;
\item enrichment reads the shipped MSigDB tables from \code{desk_paths()$genesets}
(copied there on first launch), so nothing is downloaded;
\item the 30-day KEGG auto-refresh is off, and omicsCore's offline flag is
on, unless \code{online = TRUE};
\item a pandoc placed in \code{desk_paths()$pandoc} by the bundle installer is
used for HTML reports when none is on the PATH.
}

Variables the caller set beforehand (\code{OMICSAPP_DATA_DIR},
\code{OMICSCORE_GENESET_CACHE}, \code{RSTUDIO_PANDOC}) are respected.
}
\examples{
\dontrun{
launch()
launch(project = "~/omicsDesk/projects/cheek.omp")
}
}
