% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/genesets.R
\name{sync_geneset_cache}
\alias{sync_geneset_cache}
\title{Copy the shipped gene-set tables into the user's folder}
\usage{
sync_geneset_cache(
  target = desk_geneset_dir(),
  source = bundled_geneset_dir(),
  quiet = TRUE
)
}
\arguments{
\item{target}{Destination directory. Defaults to the desk folder.}

\item{source}{Directory of shipped tables. Defaults to the package's.}

\item{quiet}{If \code{FALSE}, say what was copied and what was kept.}
}
\value{
Invisibly, a list with \code{copied}, \code{kept} (user-changed files the
bundle would have replaced) and \code{current} (already up to date).
}
\description{
Called by \code{\link[=launch]{launch()}} and \code{\link[=doctor]{doctor()}}; there is rarely a reason to call
it directly. Copies every table under \code{inst/genesets} of the installed
package into \code{desk_paths()$genesets}, skipping files that are already
identical and never overwriting a file the user has changed since it
was placed (an import, a live KEGG refresh).
}
