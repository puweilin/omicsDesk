% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/genesets.R
\name{import_geneset_cache}
\alias{import_geneset_cache}
\title{Bring gene-set tables into the desk folder}
\usage{
import_geneset_cache(path, quiet = FALSE)
}
\arguments{
\item{path}{A \code{.qs2} file or a directory containing them.}

\item{quiet}{If \code{FALSE}, report each table imported.}
}
\value{
A \code{data.frame} with one row per imported file: \code{file},
\code{database}, \code{organism}, \code{n_sets}, \code{gs_source}. Restart the app (or
the R session) afterwards: omicsCore memoises tables it has already
read.
}
\description{
The way a refreshed table reaches an offline machine: a colleague with
network access runs \code{omicsCore::refresh_geneset_cache("kegg")} and
\code{\link[=export_geneset_cache]{export_geneset_cache()}}, and the files travel on a USB stick. Accepts
one \code{.qs2} file or a directory of them. File names must follow
omicsCore's \verb{<database>__<Organism>.qs2} convention, and each table
must have the columns omicsCore reads; anything else is refused before
any file is copied.
}
\details{
Tables sourced from the live KEGG REST API are for local use only,
under KEGG's licence; the function says so when it imports one.
}
\seealso{
Other genesets: 
\code{\link{export_geneset_cache}()},
\code{\link{geneset_status}()}
}
\concept{genesets}
